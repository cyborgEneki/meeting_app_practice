import vue from 'vue';
import vuex from 'vuex';

vue.use(vuex);

const state = {
}

const mutations ={

}

const getters = {
}

const store = new vuex.Store({
    mutations,
    getters,
    state
})

export default store;